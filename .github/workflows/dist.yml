name: dist

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.6.0). If empty, reads version from Cargo.toml'
        required: false
        type: string
  push:
    tags:
      - 'v*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Verify release ref matches package version
        run: |
          set -euo pipefail
          if [[ "$GITHUB_REF" != refs/tags/v* ]]; then
            echo "::error::distribution runs must target a v-prefixed tag (ref=$GITHUB_REF)"
            exit 1
          fi

          # The package version lives in the dcg-cli member manifest.
          manifest_version="$(
            sed -n 's/^version = "\(.*\)"/\1/p' crates/dcg-cli/Cargo.toml | head -1
          )"
          if [[ -z "$manifest_version" ]]; then
            echo "::error::unable to parse the package version from crates/dcg-cli/Cargo.toml"
            exit 1
          fi
          if [[ "$GITHUB_REF_NAME" != "v${manifest_version}" ]]; then
            echo "::error::tag $GITHUB_REF_NAME does not match Cargo.toml version $manifest_version"
            exit 1
          fi

      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: nightly-2026-08-25
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Run clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Run tests
        run: cargo test --lib

  build:
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x86_64 — musl for portability. The previous gnu target
          # linked against the build runner's glibc, which on
          # ubuntu-latest is currently 2.39+ and rejects any host with
          # glibc < 2.39 (Ubuntu 22.04 LTS, RHEL 8/9, etc.). See #114.
          # `self_update`'s `rustls` feature avoids the OpenSSL link that
          # would normally make musl builds painful — dcg has no other
          # C-using deps, so musl-tools + the static target are
          # sufficient.
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            artifact_name: dcg
            apt_install: musl-tools
          # Linux ARM64 — gnu stays. musl on ARM has its own packaging
          # ecosystem we don't currently support, and the gnu build
          # runs on the native ubuntu-24.04-arm runner which keeps the
          # binary kernel-compatible with every aarch64 distro we ship
          # to. See #112 for the v0.5.1 release where the aarch64 asset
          # was actually an x86-64 binary — fixed by the native ARM
          # runner already in this matrix.
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            artifact_name: dcg
          # macOS Intel
          - os: macos-15-intel
            target: x86_64-apple-darwin
            artifact_name: dcg
          # macOS Apple Silicon
          - os: macos-14
            target: aarch64-apple-darwin
            artifact_name: dcg
          # Windows x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: dcg.exe
          # Windows on ARM (Snapdragon X / Surface). Cross-compiled on the x64
          # runner (the aarch64 target is installed via `targets:`); the PE32+
          # magic check + execute-smoke are x64-host-only and correctly skip this
          # cross-built artifact (matrix.target guards them).
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            artifact_name: dcg.exe

    runs-on: ${{ matrix.os }}
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7
        with:
          # `git describe --tags` is release evidence, not decoration. A
          # shallow build checkout can lose the tag/history needed to embed
          # the exact release provenance (#344).
          fetch-depth: 0

      - name: Install Rust nightly
        # rust-toolchain.toml pins channel = "nightly-2026-08-25", so every
        # `cargo build --target ...` below is forced onto THAT nightly. The
        # previous `@nightly` (floating latest) installed the cross-target
        # std (`rustup target add`) against the *latest* nightly instead, so
        # the pinned build could not find `core` for the non-host targets and
        # failed with `error[E0463]: can't find crate for core`. Only the two
        # cross-std targets (x86_64-unknown-linux-musl, aarch64-pc-windows-msvc)
        # were affected — every native-arch build passed — but because the
        # `release` job has `needs: build`, those two failures skipped the
        # GitHub-Actions publish for v0.6.4 and forced an out-of-band fallback
        # that shipped wrong-arch binaries (aarch64-linux got an x86-64 binary,
        # x86_64-darwin got an arm64 binary; #174). Install the SAME pinned
        # nightly here and add the target std to it. Keep this channel in sync
        # with rust-toolchain.toml.
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: nightly-2026-08-25
          targets: ${{ matrix.target }}

      - name: Install apt build deps (Linux)
        if: matrix.apt_install != ''
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends ${{ matrix.apt_install }}

      - name: Build release binary
        # DCG_RELEASE_BUILD marks release-pipeline provenance (#320): the
        # binary can then prove it is a published release rather than a local
        # build ahead of its tag when Git metadata is unavailable. The exact
        # tag checks below remain authoritative when metadata is present.
        # Read at compile time via option_env! in src/update.rs.
        env:
          DCG_RELEASE_BUILD: "1"
        run: cargo build --release --target ${{ matrix.target }}

      - name: Verify embedded release tag (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          binary="target/${{ matrix.target }}/release/${{ matrix.artifact_name }}"
          if [[ ! -x "$binary" ]]; then
            echo "::error::expected runnable release binary not found at $binary"
            exit 1
          fi

          version_output="$("$binary" --version 2>&1)"
          embedded_describe="$(
            printf '%s\n' "$version_output" |
              sed -nE 's/.*Commit:[[:space:]]*([^[:space:]]+).*/\1/p' |
              head -1
          )"
          if [[ "$embedded_describe" != "$GITHUB_REF_NAME" ]]; then
            echo "::error::embedded git describe must equal release tag exactly: expected $GITHUB_REF_NAME, got '${embedded_describe:-<missing>}'"
            printf '%s\n' "$version_output"
            exit 1
          fi
          echo "embedded release provenance verified: $embedded_describe"

      - name: Verify Linux x86_64 musl binary has no glibc symbols
        if: matrix.target == 'x86_64-unknown-linux-musl'
        run: |
          set -euo pipefail
          # Catch a future regression where the musl matrix entry
          # silently links against glibc anyway. `objdump -T` on a
          # static musl binary prints "no dynamic symbol table" to
          # stderr and exits 0; the grep against `GLIBC_` is the
          # actual gate. Any GLIBC_* symbol surfacing here is a
          # release-blocker (see #114 for the original regression).
          # We pipe stderr to stdout so the dump-failure case (missing
          # binary, unreadable file) shows up in the job log instead
          # of getting silently swallowed.
          binary="target/${{ matrix.target }}/release/${{ matrix.artifact_name }}"
          # Explicit existence check: without it, a missing binary
          # would make `objdump -T <missing> | grep -q GLIBC_` return
          # exit 1 (no match), the `if` would evaluate to false, and
          # we'd echo "verified" against a binary that doesn't exist.
          # That false negative is a release-engineering footgun
          # (cargo could legitimately skip the build under caching
          # weirdness), so guard the gate against it directly.
          if [ ! -f "$binary" ]; then
            echo "::error::expected built binary not found at $binary"
            exit 1
          fi
          if objdump -T "$binary" 2>&1 | grep -q "GLIBC_"; then
            echo "::error::musl binary unexpectedly contains GLIBC symbols — release would re-introduce #114"
            objdump -T "$binary" | grep "GLIBC_" || true
            exit 1
          fi
          echo "musl binary verified: no GLIBC_* symbols present"

      - name: Verify Linux aarch64 binary is actually aarch64
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          set -euo pipefail
          # Pin against the v0.5.1 regression (#112): the published
          # aarch64 tarball contained an x86-64 ELF. Native ARM
          # runners in this matrix make that impossible by
          # construction, but the file-type check is cheap insurance.
          binary="target/${{ matrix.target }}/release/${{ matrix.artifact_name }}"
          if [ ! -f "$binary" ]; then
            echo "::error::expected built binary not found at $binary"
            exit 1
          fi
          arch_str="$(file "$binary")"
          if ! echo "$arch_str" | grep -q "aarch64"; then
            echo "::error::aarch64 build produced a non-aarch64 binary — release would re-introduce #112"
            echo "$arch_str"
            exit 1
          fi
          echo "aarch64 binary verified: $arch_str"

      - name: Create tarball (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          TARBALL="dcg-${{ matrix.target }}.tar.xz"
          cd target/${{ matrix.target }}/release
          tar -cJf "../../../dist/$TARBALL" ${{ matrix.artifact_name }}
          cd ../../../dist
          shasum -a 256 "$TARBALL" > "$TARBALL.sha256"

      - name: Verify Windows binary is actually PE32+
        if: contains(matrix.target, 'pc-windows-msvc')
        shell: pwsh
        run: |
          # Pin against the v0.5.1 regression (#115): the published Windows
          # assets were Linux ELF binaries renamed to `dcg.exe`. The GH
          # Actions test job had failed and a fallback build path
          # (cross-compile on a non-Windows host) silently produced ELF
          # output and packaged it as Windows. windows-latest runs natively
          # so cargo cannot produce ELF here by construction, but checking
          # the magic bytes is cheap insurance against a future regression
          # (e.g. a cached target/ leaking the wrong artifact).
          $binary = "target/${{ matrix.target }}/release/${{ matrix.artifact_name }}"
          if (-not (Test-Path $binary)) {
            Write-Output "::error::expected built binary not found at $binary"
            exit 1
          }
          $magic = [System.IO.File]::ReadAllBytes($binary)[0..1]
          $mz = [BitConverter]::ToString($magic)
          if ($mz -ne "4D-5A") {
            Write-Output "::error::Windows build produced a non-PE32+ binary (magic=$mz, expected 4D-5A 'MZ') — release would re-introduce #115"
            exit 1
          }
          Write-Output "Windows binary verified: PE32+ magic 'MZ' present"

      - name: Verify embedded release tag (Windows ARM64 cross-build)
        if: matrix.target == 'aarch64-pc-windows-msvc'
        shell: pwsh
        run: |
          # The x64 runner cannot execute the ARM64 binary. Inspect the PE
          # bytes instead: the exact tag must be embedded, while tag-plus-'-'
          # rejects both dirty and ahead `git describe` spellings (#344).
          $ErrorActionPreference = 'Stop'
          $binary = "target/${{ matrix.target }}/release/${{ matrix.artifact_name }}"
          if (-not (Test-Path -LiteralPath $binary -PathType Leaf)) {
            Write-Output "::error::expected cross-built release binary not found at $binary"
            exit 1
          }

          function Find-ByteSequence {
            param(
              [byte[]] $Haystack,
              [byte[]] $Needle
            )
            if ($Needle.Length -eq 0 -or $Needle.Length -gt $Haystack.Length) {
              return $false
            }
            for ($i = 0; $i -le $Haystack.Length - $Needle.Length; $i++) {
              $matches = $true
              for ($j = 0; $j -lt $Needle.Length; $j++) {
                if ($Haystack[$i + $j] -ne $Needle[$j]) {
                  $matches = $false
                  break
                }
              }
              if ($matches) { return $true }
            }
            return $false
          }

          $bytes = [System.IO.File]::ReadAllBytes($binary)
          $tagBytes = [System.Text.Encoding]::ASCII.GetBytes($env:GITHUB_REF_NAME)
          $tagWithSuffix = [System.Text.Encoding]::ASCII.GetBytes("$env:GITHUB_REF_NAME-")
          if (-not (Find-ByteSequence -Haystack $bytes -Needle $tagBytes)) {
            Write-Output "::error::cross-built binary does not embed release tag $env:GITHUB_REF_NAME"
            exit 1
          }
          if (Find-ByteSequence -Haystack $bytes -Needle $tagWithSuffix) {
            Write-Output "::error::cross-built binary embeds dirty/ahead provenance beginning with $env:GITHUB_REF_NAME-"
            exit 1
          }
          Write-Output "embedded ARM64 release provenance verified: $env:GITHUB_REF_NAME"

      - name: Execute Windows binary (allow + deny + --version)
        if: matrix.target == 'x86_64-pc-windows-msvc'
        shell: pwsh
        run: |
          # The PE32+ check above proves it's a Windows binary; this proves it
          # actually RUNS. A binary that is a valid PE yet crashes on startup must
          # not ship. Fast: --version + one allow + one deny.
          $ErrorActionPreference = 'Stop'
          $bin = "target/${{ matrix.target }}/release/${{ matrix.artifact_name }}"

          $ver = (& $bin --version 2>&1 | Out-String)
          if ($LASTEXITCODE -ne 0) { Write-Output "::error::dcg.exe --version failed (exit $LASTEXITCODE)"; exit 1 }
          Write-Output "version: $($ver.Trim())"
          $commitMatch = [regex]::Match($ver, 'Commit:\s+(?<describe>[^\s│]+)')
          if (-not $commitMatch.Success) {
            Write-Output "::error::dcg.exe --version did not report embedded git provenance"
            exit 1
          }
          $embeddedDescribe = $commitMatch.Groups['describe'].Value
          if ($embeddedDescribe -cne $env:GITHUB_REF_NAME) {
            Write-Output "::error::embedded git describe must equal release tag exactly: expected $env:GITHUB_REF_NAME, got '$embeddedDescribe'"
            exit 1
          }

          # ALLOW: a safe command must produce empty stdout (silent exit 0).
          $allow = ('{"tool_name":"Bash","tool_input":{"command":"git status"}}' | & $bin | Out-String)
          if (-not [string]::IsNullOrWhiteSpace($allow)) {
            Write-Output "::error::expected silent ALLOW for 'git status', got: $allow"; exit 1
          }

          # DENY: a destructive command must emit a deny decision. The destructive
          # string is base64'd so it never appears literally in the workflow file
          # (and cannot trip any scanning hook): decodes to 'git reset --hard'.
          $cmd = [Text.Encoding]::UTF8.GetString([Convert]::FromBase64String('Z2l0IHJlc2V0IC0taGFyZA=='))
          $deny = (([pscustomobject]@{ tool_name = 'Bash'; tool_input = [pscustomobject]@{ command = $cmd } } | ConvertTo-Json -Compress) | & $bin | Out-String)
          if ($deny -notmatch 'permissionDecision' -or $deny -notmatch 'deny') {
            Write-Output "::error::expected DENY JSON for the destructive command, got: $deny"; exit 1
          }
          Write-Output "Windows binary executes: --version OK, allow OK, deny OK"

      - name: Create zip (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          $zipName = "dcg-${{ matrix.target }}.zip"
          Compress-Archive -Path "target/${{ matrix.target }}/release/${{ matrix.artifact_name }}" -DestinationPath "dist/$zipName"
          cd dist
          $hash = (Get-FileHash -Algorithm SHA256 $zipName).Hash.ToLower()
          "$hash  $zipName" | Out-File -Encoding ASCII "$zipName.sha256"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.target }}
          path: dist/*

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - uses: actions/checkout@v7

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: List artifacts
        run: ls -laR dist

      - name: Install cosign
        uses: sigstore/cosign-installer@v4.1.2

      - name: Sign release artifacts (sigstore bundle)
        run: |
          set -euo pipefail
          shopt -s nullglob
          cd dist
          for f in dcg-*.tar.xz dcg-*.zip; do
            cosign sign-blob --yes --bundle "${f}.sigstore.json" "$f"
          done

      - name: Copy install scripts to dist
        run: |
          cp install.sh dist/ 2>/dev/null || echo "No install.sh found"
          cp install.ps1 dist/ 2>/dev/null || echo "No install.ps1 found"

      - name: Checksum + sign install scripts (git_safety_guard-ythp)
        # Publishes install.sh.sha256 / install.ps1.sha256 (with the
        # `<sha>  <basename>` format `shasum -a 256 -c` understands) plus
        # cosign sigstore bundles. `dcg update` downloads the script to a
        # tempfile, fetches the sha256, verifies, and only then exec's it.
        # Without this the installer-fetch step is a supply-chain trust
        # window even when pinned to a tag.
        run: |
          set -euo pipefail
          shopt -s nullglob
          cd dist
          for script in install.sh install.ps1; do
            [ -f "$script" ] || continue
            shasum -a 256 "$script" > "$script.sha256"
            cosign sign-blob --yes --bundle "$script.sigstore.json" "$script"
          done

      - name: Determine release tag
        id: tag
        run: |
          if [[ -n "${{ github.event.inputs.tag }}" ]]; then
            echo "tag=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          elif [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          else
            version=$(sed -n 's/^version = "\(.*\)"/\1/p' crates/dcg-cli/Cargo.toml | head -1)
            echo "tag=v${version}" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: ${{ steps.tag.outputs.tag }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify-homebrew-tap:
    name: Notify Homebrew Tap
    runs-on: ubuntu-latest
    needs: release
    timeout-minutes: 5
    continue-on-error: true
    env:
      HAS_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN != '' }}
    steps:
      - name: Check for token
        id: check
        run: |
          if [[ "${{ env.HAS_TOKEN }}" != "true" ]]; then
            echo "::warning::HOMEBREW_TAP_TOKEN not configured, skipping notification"
            echo "skip=true" >> $GITHUB_OUTPUT
          fi

      - name: Extract version from tag
        if: steps.check.outputs.skip != 'true'
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Trigger formula update
        if: steps.check.outputs.skip != 'true'
        uses: peter-evans/repository-dispatch@28959ce8df70de7be546dd1250a005dd32156697 # v4.0.1
        with:
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          repository: quangdang46/homebrew-tap
          event-type: formula-update
          client-payload: |
            {
              "tool": "dcg",
              "version": "${{ steps.version.outputs.version }}"
            }

  notify-scoop-bucket:
    name: Notify Scoop Bucket
    runs-on: ubuntu-latest
    needs: release
    timeout-minutes: 5
    continue-on-error: true
    env:
      HAS_TOKEN: ${{ secrets.SCOOP_BUCKET_TOKEN != '' }}
    steps:
      - name: Check for token
        id: check
        run: |
          if [[ "${{ env.HAS_TOKEN }}" != "true" ]]; then
            echo "::warning::SCOOP_BUCKET_TOKEN not configured, skipping notification"
            echo "skip=true" >> $GITHUB_OUTPUT
          fi

      - name: Extract version from tag
        if: steps.check.outputs.skip != 'true'
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Trigger manifest update
        if: steps.check.outputs.skip != 'true'
        uses: peter-evans/repository-dispatch@28959ce8df70de7be546dd1250a005dd32156697 # v4.0.1
        with:
          token: ${{ secrets.SCOOP_BUCKET_TOKEN }}
          repository: quangdang46/scoop-bucket
          event-type: manifest-update
          client-payload: |
            {
              "tool": "dcg",
              "version": "${{ steps.version.outputs.version }}"
            }
